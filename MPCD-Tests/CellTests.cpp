#include "pch.h"

#include "Cell.h"
#include <Eigen/Dense>

using namespace MPCD;
using namespace Eigen;

/*
TEST(Cell, DISABLED_OperatorPlus) {
	Cell cell1;
	Cell cell2;
	Vector2d pos(0, 0);
	Vector2d vel1(1, 0);
	Particle p1(pos, vel1);
	cell1.add(p1);
	Vector2d vel2(0, 1);
	Particle p2(pos, vel2);
	cell2.add(p2);
	Cell cell = cell1 + cell2;
	
}
*/