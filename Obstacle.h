#pragma once

#ifndef OBSTACLE_H
#define OBSTACLE_H
#include <map>
namespace MPCD {
	class Obstacle
	{
	public:
		//virtual std::map<std::pair<int, int>, bool> occupied();
	private:
		//std::map<std::pair<int, int>, bool> _occupied;
	};
}
#endif // !OBSTACLE_H
